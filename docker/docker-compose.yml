services:
  redis:
    image: redis:7
    ports: ["6379:6379"]
    volumes:
      - redis_data:/data
    restart: unless-stopped

  # postgres:
  #   image: postgres:16
  #   environment:
  #     POSTGRES_DB: ${DB_DATABASE}
  #     POSTGRES_USER: ${DB_USERNAME}
  #     POSTGRES_PASSWORD: ${DB_PASSWORD}
  #   ports: ["5432:5432"]
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   restart: unless-stopped

  # PostgreSQL Admin Interface
  # pgadmin:
  #   image: dpage/pgadmin4:latest
  #   environment:
  #     PGADMIN_DEFAULT_EMAIL: admin@sfmiddleware.com
  #     PGADMIN_DEFAULT_PASSWORD: admin123
  #     PGADMIN_CONFIG_SERVER_MODE: 'False'
  #   ports: ["5050:80"]
  #   volumes:
  #     - pgadmin_data:/var/lib/pgadmin
  #   depends_on:
  #     - postgres
  #   restart: unless-stopped

  # Alternative: Adminer (lighter PostgreSQL admin)
  # adminer:
  #   image: adminer:latest
  #   ports: ["8081:8080"]
  #   depends_on:
  #     - postgres
  #   restart: unless-stopped

  jaeger:
    image: jaegertracing/all-in-one:latest
    ports: ["16686:16686"]
    restart: unless-stopped

  # wiremock:
  #   image: wiremock/wiremock:latest
  #   ports: ["8082:8081"]
  #   volumes: 
  #     - ./docker/wiremock:/home/wiremock
  #   environment:
  #     - WIREMOCK_OPTIONS=--global-response-templating --verbose
  #   restart: unless-stopped
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:8082/__admin/health"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 3
  #     start_period: 40s

volumes:
  # postgres_data:
  redis_data:
  pgadmin_data: